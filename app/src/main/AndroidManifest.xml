<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!--
      USE_FULL_SCREEN_INTENT 는 선언하지 않습니다.
      Play 가 알람·통화 앱으로 제한하는 권한인데, 이 앱에서 화면을 덮는 창은
      '다른 앱 위에 표시'(SYSTEM_ALERT_WINDOW) 로 띄우므로 필요가 없습니다.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
      REQUEST_IGNORE_BATTERY_OPTIMIZATIONS 는 의도적으로 선언하지 않습니다.
      Play 정책상 허용 사례가 좁아 반려 위험이 큰 권한입니다(docs/play-review.md 5항).
      대신 설정 화면에서 앱 정보로 보내 사용자가 직접 '제한 없음' 을 고르게 합니다.
      현재 상태를 읽는 PowerManager.isIgnoringBatteryOptimizations 는 권한이 필요 없어
      경고 표시는 그대로 동작합니다.
    -->
    <!-- 미리 알림을 제 시각에 울리기 위한 권한. 없으면 몇 분 늦는 부정확 알람으로 대체됩니다. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoLock">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- 한 달을 펼쳐 보는 화면. 목록 화면의 달력 버튼으로 엽니다. -->
        <activity
            android:name=".CalendarActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- 위젯에서 앱을 열지 않고 그 자리에서 추가·수정하기 위한 투명 액티비티 -->
        <activity
            android:name=".QuickAddActivity"
            android:exported="false"
            android:theme="@style/Theme.TodoLock.Popup"
            android:taskAffinity="com.hj0128.todolock.quickadd"
            android:excludeFromRecents="true"
            android:windowSoftInputMode="adjustResize" />

        <!-- 위젯 톱니바퀴로 열리는 겉모습 설정 -->
        <activity
            android:name=".WidgetSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.TodoLock.Popup"
            android:taskAffinity="com.hj0128.todolock.widgetsettings"
            android:excludeFromRecents="true" />

        <!-- 미리 알림 방식이 '전체 팝업' 일 때 뜨는 창 -->
        <activity
            android:name=".ReminderPopupActivity"
            android:exported="false"
            android:theme="@style/Theme.TodoLock.Popup"
            android:launchMode="singleInstance"
            android:taskAffinity="com.hj0128.todolock.remindpopup"
            android:excludeFromRecents="true" />

        <activity
            android:name=".TodayPopupActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:taskAffinity="com.hj0128.todolock.popup"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.TodoLock.Popup" />

        <service
            android:name=".UnlockService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Listens for device unlock to surface the user's todo list for today" />
        </service>

        <!-- 프로세스가 죽어도 알람으로 깨어나 감지 서비스를 되살립니다 -->
        <receiver
            android:name=".Watchdog"
            android:exported="false" />

        <!-- 할 일별 미리 알림 알람과, 그 알림의 '완료' 버튼 -->
        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />

        <!-- 홈 화면 위젯 -->
        <receiver
            android:name=".TodoWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/todo_widget_info" />
        </receiver>

        <!-- 위젯 목록에 행을 공급합니다. 런처가 바인딩하므로 이 권한이 필요합니다. -->
        <service
            android:name=".TodoWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!--
          날이 바뀌면 위젯의 '오늘/내일' 표기를 다시 맞춥니다.
          평소에는 스스로 잡아 둔 자정 알람이 깨우고, 사용자가 시계나 시간대를
          바꾼 경우에는 아래 시스템 알림으로 받습니다(모두 시스템만 보낼 수 있는
          보호된 방송이라 exported 여도 남이 부를 수 없습니다).
        -->
        <receiver
            android:name=".DayRollover"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
